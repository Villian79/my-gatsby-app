import { Hero } from "./hero"
import { CallToAction } from "./callToAction"
import { PriceOptions } from "./priceOptions"
import { Form } from "./form"

export const components = {
  hero: Hero,
  call_to_action: CallToAction,
  pricing_options: PriceOptions,
  form: Form,
}
